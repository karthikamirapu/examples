{
  "type": "interpreter-config",
  "configurations": [
    {
      "settings": [
        {
          "driverOptions": {
            "host": "ondemand.saucelabs.com",
            "port": 80
          },
          "browserOptions":  {
            "platform": "${SAUCE_PLATFORM}",
            "browserName": "${SAUCE_BROWSER}",
            "version": "${SAUCE_BROWSER_VERSION}"
            "screenResolution": "1280x1024"
            }
        }
      ],
      "scripts": [
        "Assetclass-OG/PDP.json"
      ]
    }
  ]
}
